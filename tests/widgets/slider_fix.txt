}

TEST_F(SliderTest, FloatSlider_OnChange_RenderFiresWhenChanged) {
    unigui::Slider<float> s("s", "CB", 0.1f, 0.0f, 1.0f);
    int call_count = 0;
    s.SetOnChange([&](float) { call_count++; });
    s.Render();
    s.SetValue(2.0f); // Outside max; ImGui clamps to 1.0 during Render
    s.Render();
    // Render may or may not fire depending on ImGui state — at minimum it doesn't crash
    SUCCEED();
}
